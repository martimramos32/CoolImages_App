<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Internet permission required for Unsplash API calls and image loading -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_gallery_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_gallery_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.MIP2tp2">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.ImageDetailActivity"
            android:exported="false"
            android:label="Image Details"
            android:theme="@style/Theme.MIP2tp2" />

        <activity
            android:name=".ui.FavoritesActivity"
            android:exported="false"
            android:label="Favorites"
            android:theme="@style/Theme.MIP2tp2" />

    </application>

</manifest>
